import React, { ReactNode } from 'react'
import { Params } from '@types'

const ProductDetails = (param: any) => {
  return (
    <div>
      <h1 className=''>{param.title}</h1>
    </div>
  )
}

export default ProductDetails
